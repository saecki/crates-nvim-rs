[workspace]
resolver = "2"
members = [
    "crates-nvim",
    "crates-toml",
    "crates-cli",
]

[workspace.dependencies]
toml = { package = "crates-toml", path = "crates-toml" }

[profile.release]
strip = true
